# Dev Plan Generator — Pages deploy package

Self-contained build of `../../proto-devplan.html` (assets bundled, paths local). To publish: create public repo `bhb-devplan-generator`, upload the contents of this folder (`index.html` + `assets/`), enable GitHub Pages (main branch, root). Regenerate from the module after edits — never hand-edit this copy. Divergence policy: `design/10-repo-and-hosting-plan.md`.
